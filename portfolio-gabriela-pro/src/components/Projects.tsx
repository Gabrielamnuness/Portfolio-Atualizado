import { projects } from '../data/site';import { ExternalLink } from 'lucide-react';
export default function Projects(){return <section id="projetos" className="px-6 py-28"><div className="mx-auto max-w-7xl"><p className="section-subtitle">Projetos</p><h2 className="section-title">Projetos para destacar no portfólio</h2><p className="mt-5 max-w-3xl text-slate-400 leading-7">A ideia é apresentar cada projeto como estudo de caso: problema, processo, dados/requisitos, solução, resultado e aprendizados.</p><div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{projects.map((p)=><article key={p.title} className="card group flex min-h-[300px] flex-col p-7"><div className="mb-5 text-sm font-semibold text-cyan-300">{p.type}</div><h3 className="text-2xl font-black">{p.title}</h3><p className="mt-4 flex-1 leading-7 text-slate-400">{p.desc}</p><div className="mt-6 flex flex-wrap gap-2">{p.tech.map(t=><span key={t} className="rounded-full bg-cyan-300/10 px-3 py-1 text-xs font-semibold text-cyan-200">{t}</span>)}</div><a href={p.link} className="mt-6 inline-flex items-center gap-2 font-bold text-cyan-300">Ver estudo de caso <ExternalLink size={16}/></a></article>)}</div></div></section>}
